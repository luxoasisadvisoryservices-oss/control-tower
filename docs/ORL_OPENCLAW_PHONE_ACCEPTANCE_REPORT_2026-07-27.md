# ORL OpenClaw Phone Acceptance Report - 2026-07-27

## Verdict

Acceptance is not green yet.

The OpenClaw ORL operator deployment contract and skill proposal exist, but the live Hetzner install and phone-channel acceptance could not be verified from this session because the only known private SSH route, `100.67.211.60`, timed out on port 22.

## Requested Acceptance

Damiano asks the OpenClaw channel:

```text
analyse Paramount and make me her P&L
```

Required result:

- OpenClaw chains ORL skills instead of answering as generic chat.
- Tenant scoping is enforced.
- P&L output is a correct file or an honest refusal under the fixed ORL rules.
- Cross-tenant regression run through OpenClaw is green.
- Client channels remain on the existing hardened path, with no client traffic through OpenClaw until Damiano explicitly orders it after acceptance.

## Install Location And Isolation

Planned install location:

```text
/srv/openclaw-orl
```

Planned service user:

```text
openclaw-orl
```

Planned systemd service:

```text
openclaw-orl.service
```

Required isolation:

- own OS user: `openclaw-orl`;
- own workspace, state, env and logs under `/srv/openclaw-orl`;
- no read access to `/root/oasis-agents`;
- no read access to `/srv/orl-dev`, `/srv/orl-production`, ORL engine code, DBs, raw exports, client `.env` files or connector secrets;
- OpenClaw skills call only tenant-scoped ORL operator API endpoints;
- ORL numbers only.

Current proof status:

- Deployment contract exists: `ORL_OPENCLAW_ISOLATED_OPERATOR_DEPLOYMENT_2026-07-27.md`;
- skill proposal exists: `orl-operator-api-20260727-8cf9162266`;
- live service proof is missing because Hetzner access timed out.

## Skill List

The ORL OpenClaw skill contract covers:

- `orl-metric-grids` - occupancy, ADR, RevPAR, pace, ranking, pricing floor, source freshness and validated metric grids;
- `orl-pnl-artefacts` - P&L, CSV, XLSX, PDF, action sheets and post-fix report artefacts generated by ORL API;
- `orl-briefs` - daily, weekly, owner and one-screen operator briefs;
- `orl-tasks-approvals` - tasks, follow-ups, recommendation ledger entries and approval cards;
- `orl-tenants` - tenant list and explicit tenant switching;
- `orl-funded-research` - research only when the ORL API reports it is funded and enabled.

Every skill contract says: ORL numbers only. Use tenant-scoped ORL operator API. Never read engine code, DBs, raw data or secrets.

## Transcript Evidence

No live phone transcript exists yet.

Attempted server route:

```text
ssh -o BatchMode=yes -o ConnectTimeout=8 -i /Users/damianoagent/.ssh/id_ed25519 root@100.67.211.60 ...
Result: ssh: connect to host 100.67.211.60 port 22: Operation timed out

ssh -o BatchMode=yes -o ConnectTimeout=8 -i /Users/damianoagent/.ssh/id_ed25519 damianoagent@100.67.211.60 ...
Result: ssh: connect to host 100.67.211.60 port 22: Operation timed out
```

Because the service cannot be reached, there is no valid transcript for:

- `analyse Paramount and make me her P&L`;
- ORL P&L artefact generation;
- tenant-switch refusal;
- cross-tenant regression through OpenClaw.

## Regression Results

| Check | Status | Evidence |
| --- | --- | --- |
| Isolated `openclaw-orl` service exists | Not proven | Hetzner SSH timed out |
| `/srv/openclaw-orl` workdir exists | Not proven | Hetzner SSH timed out |
| `openclaw-orl` cannot read `/root/oasis-agents` secrets | Not proven | Requires server command |
| Telegram OpenClaw channel routes to ORL operator | Not proven | Requires live bot/channel |
| `analyse Paramount and make me her P&L` calls P&L artefact skill | Not proven | Requires live OpenClaw channel |
| Correct file or honest refusal under fixed rules | Not proven | Requires ORL API response |
| Cross-tenant regression through OpenClaw | Not green | Not runnable without live channel/server |
| Client channels stay on hardened path | Preserved | No client channel changes made |

## Boundary Status

Client channels stay on the existing hardened path.

No client traffic has been routed through OpenClaw. No client connector, client Telegram, Slack, Gmail, Drive, raw data, `.env`, DB or ORL engine path was connected to this OpenClaw operator lane.

## Next Required Proof

Run the acceptance only after Hetzner shell/node access is available:

1. Verify `id openclaw-orl`, `/srv/openclaw-orl`, file permissions and `systemctl status openclaw-orl`.
2. Verify `openclaw-orl` cannot read `/root/oasis-agents` or client env files.
3. Send the Telegram prompt: `analyse Paramount and make me her P&L`.
4. Capture the OpenClaw transcript and ORL API audit log showing tenant, user, endpoint, RevenueState version, evidence and policy result.
5. Run the cross-tenant regression through OpenClaw and record pass/fail.

